# Αντίγραψέ το σε .env και συμπλήρωσε τιμές.
# Δημιουργία μυστικού: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PAYLOAD_SECRET=
DATABASE_URI=file:./argolidastay.db
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
